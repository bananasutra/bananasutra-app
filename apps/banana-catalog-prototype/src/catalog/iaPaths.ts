import { canonicalPathForRoute } from './seoPaths'

/** About anchor for the sutra matrix (canonical sutra hub entry point). */
export const ABOUT_SUTRAS_HREF = `${canonicalPathForRoute('/about')}#sutras` as const
