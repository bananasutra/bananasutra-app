# Shared Library

Use this folder for:

- shared types (song cards, facets, detail payloads)
- data normalization helpers
- sorting and ranking utilities
- **`analytics.ts`** — typed GA4 custom events (W-002); SPA `page_view` stays in `useAnalyticsPageView.ts`

