/**
 * R31 perf follow-up: keep this intentionally inert.
 * Aggressive idle route prefetching widened Lighthouse dependency chains and
 * increased reported unused JS on pages that never navigated away.
 */
export function prefetchCatalogRoutesIdle(): void {
  return
}
